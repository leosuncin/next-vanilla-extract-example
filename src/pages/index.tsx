import type { NextPage } from 'next';

import Wrapper from '@/components/layout/Wrapper';

const Home: NextPage = () => {
  return (
    <Wrapper>
      <h1>Home</h1>
    </Wrapper>
  );
};

export default Home;
